<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="MemoryCards.StudyMode"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MemoryCards"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="StudyMode">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="2*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="1">

            <ListView x:Name="ListView" ItemsSource="{Binding Cards}"></ListView>

            <StackPanel Orientation="Horizontal" Grid.Row="1" VerticalAlignment="Bottom" HorizontalAlignment="Center">
                <TextBox x:Name="QuestionTextBox" Text="{Binding SelectedCard}" PlaceholderText="Kérdés"></TextBox>
                <TextBox x:Name="AnswerTextBox" PlaceholderText="Válasz"></TextBox>
                <StackPanel/>
        </Grid>
    </Grid>
</Window>
